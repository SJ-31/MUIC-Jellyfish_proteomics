nextflow_process {

    name "Test Process COVERAGE_CALC"
    script "modules/coverage.nf"
    process "COVERAGE_CALC"

    test("Should run without failures") {

        when {
            params {
                pref = "C_indra"
                pass = "1-First_pass"
                // pass = "2-Second_pass"

                combined = "$baseDir/tests/toy_sample.tsv"
                outdir = "$baseDir/tests/nf-test-out/coverage_merge"
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.fromPath(params.combined)
                input[1] = params.outdir
                """
            }
        }

        then {
            assert process.success
        }

    }

}
