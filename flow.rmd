# Document Title

```{r}
library(DiagrammeR)
grViz("
digraph boxes_and_circles {
  node [fontname='Helvetica,Arial,sans-serif';
    style=filled]

  subgraph sep {
  node [shape=folder]; rawRNA1; rawRNA2; mtdna;
  rawRNA1 [label='C. fleckeri']
  rawRNA2 [label='C. yamaguchii']
  mtdna [label='Cnidarian mtDNA']

  node [shape=component]; fp; bbduk; spades; tdc; busco;
  mtdna->bbduk;
  {rawRNA1,rawRNA2}->fp label
  bbduk->spades [label='Contaminant filtering']
  spades [label='RNASpades\nTranscriptome denovo assembly']
  bbduk[label='BBduk']
  }

  subgraph cluster1 {
  raw->thermoparse->mgf->{csnv,sms,pepnet}->denovo
  thermoparse->mzml
  mzml->{flcn,wiz}
  {tdc,unip,ncbi,denovo}->decoy_db
  }

  subgraph cluster2 {
  {decoy_db,mzml}->{comet,mq,idpy,msfrag,meta,tide}
  mq->ms2
  {comet,idpy,msfrag,ms2,meta,tide}->perc
  }

  subgraph cluster_e {
  rank = same
  comet; mq; idpy; msfrag; meta; tide
  }

  subgraph 3 {
  {comet,idpy,msfrag}->bk_decoy->decoy_db
  }

  perc [shape=star, fill=red]
  perc->{ursgal,intersect}
  {ursgal,intersect}->{interpro,api}
  perc->{empai,dlfq,flfq}
}
")
```
